package Sorting;

public class countingsort_stable {

    // int[] temp = new int[n];
    // for (int k=n-1;k>=0;k--)
    // {
    //     temp[--countingArray[getDigit(position,input[k],radix)]] = input[k];
    // }
    
}
/*
to know how many values have less than or equal to the value at a current index, sum up the counts before the index in the counting array
*/
